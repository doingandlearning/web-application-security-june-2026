# Module 4 — Demos

| Demo | Description |
|------|-------------|
| `00-demo-setup.md` | Start TrustyTickets and confirm the Module 3 payloads still work (baseline) |
| `01-demo-fix-sqli.md` | Fix SQLi in `TicketService` (tickets search + reports) and re-test payload |
| `02-demo-fix-idor.md` | Fix IDOR in `TicketsController` + `AttachmentsController` and re-test |
| `03-demo-fix-xss.md` | Fix stored XSS in `wwwroot/ticket.html` (comments + description) and re-test |
| `04-demo-add-validation.md` | Add basic validation to reduce 500s and tighten input constraints |

Add one `.md` per demo; use `prompts/` or code samples as needed.
